import { Container, LoadingContent } from './loading.styles'

const Loading = () => {
    return (
        <Container>
            <LoadingContent />
        </Container>
    )
}

export default Loading
